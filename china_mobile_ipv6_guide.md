# 中国移动宽带IPv6配置完整指南

## 🎯 问题分析

您的阿里云DDNS IPv6解析成功了，但仍无法远程访问，这通常是以下几个环节的问题：

1. **光猫IPv6配置问题** - 最常见
2. **路由器IPv6设置问题** - 次常见  
3. **服务器应用绑定问题** - 应用层面
4. **防火墙阻挡问题** - 安全层面
5. **运营商策略限制** - 网络层面

## 🔍 快速检查方法

### 1. 运行检查工具
```bash
python ipv6_network_checker.py
```

### 2. 手动检查IPv6地址
```cmd
# Windows下查看IPv6地址
ipconfig

# 查找类似这样的全球单播地址：
# IPv6 地址: 2408:8207:xxxx:xxxx:xxxx:xxxx:xxxx:xxxx
```

### 3. 测试IPv6连通性
```cmd
# 测试是否能访问IPv6网站
ping -6 ipv6.google.com
ping -6 2001:4860:4860::8888
```

## 🏠 光猫配置检查 (重点！)

### 中国移动常见光猫型号配置

#### 华为光猫 (HG8xxx系列)
1. **进入光猫管理界面**
   - 浏览器打开: `http://192.168.1.1`
   - 账号: `CMCCAdmin` 密码: `aDm8H%MdA` (或设备标签密码)

2. **检查IPv6配置**
   ```
   网络 → 宽带设置 → 连接名称
   ✅ IPv6功能: 启用
   ✅ IPv6地址获取: DHCPv6 或 SLAAC
   ✅ IPv6前缀委派: 启用
   ```

3. **检查状态信息**
   ```
   状态 → 网络侧信息
   ✅ IPv6连接状态: 已连接
   ✅ IPv6地址: 2408:8207:xxxx (中国移动前缀)
   ✅ IPv6前缀: /64 或 /56
   ```

#### 中兴光猫 (F4xxx/F6xxx系列)
1. **进入管理界面**
   - 浏览器打开: `http://192.168.1.1`
   - 账号: `admin` 密码: 设备标签或 `admin`

2. **IPv6设置路径**
   ```
   网络设置 → 宽带设置 → IPv6设置
   ✅ IPv6功能: 开启
   ✅ 获取IPv6方式: 自动获取
   ✅ IPv6前缀委派: 开启
   ```

### 光猫重要配置检查项
- [ ] IPv6功能已启用
- [ ] 获得了 `2408:8207` 开头的IPv6地址
- [ ] IPv6前缀委派已启用 (关键！)
- [ ] DNS服务器包含IPv6地址

## 📡 路由器配置检查

### 通用路由器IPv6配置
1. **基本IPv6设置**
   ```
   高级设置 → IPv6设置
   ✅ IPv6功能: 启用
   ✅ 连接类型: 桥接/透传模式
   ✅ 前缀获取: 自动
   ✅ 地址分配: SLAAC + 无状态DHCPv6
   ```

2. **DHCP v6设置**
   ```
   ✅ DHCPv6服务: 启用
   ✅ 前缀委派: 启用  
   ✅ DNS分发: 启用
   ```

### 各品牌路由器具体设置

#### 小米路由器
```
高级设置 → 外网设置 → IPv6设置
- IPv6: 开启
- 配置方式: 自动配置
- IPv6 DNS: 自动获取
```

#### 华为路由器  
```
更多功能 → 网络设置 → IPv6设置
- IPv6开关: 开启
- 获取方式: 自动获取
- LAN侧IPv6: 开启
```

#### TP-Link路由器
```
高级设置 → IPv6 → 快速设置
- IPv6: 启用
- 互联网连接类型: 桥接
- 前缀委派: 启用
```

## 🖥️ 服务器应用配置

### 确保应用绑定IPv6地址

1. **前端应用 (端口8080)**
   ```javascript
   // 如果是Node.js应用
   app.listen(8080, '::', () => {
       console.log('Server running on [::]:8080');
   });
   ```

2. **后端应用 (端口5030)**
   ```python
   # 如果是Python Flask/FastAPI
   if __name__ == '__main__':
       app.run(host='::', port=5030, debug=True)
   ```

3. **Martin服务 (端口3000)**
   ```bash
   # 确保Martin配置绑定到IPv6
   martin --host :: --port 3000
   ```

### 检查应用是否正确监听
```cmd
# Windows下检查端口监听
netstat -an | findstr :8080
netstat -an | findstr :5030  
netstat -an | findstr :3000

# 应该看到类似：
# TCP    [::]:8080             [::]:0                 LISTENING
```

## 🛡️ 防火墙配置

### Windows防火墙设置
1. **添加入站规则**
   ```cmd
   # 管理员权限运行
   netsh advfirewall firewall add rule name="Allow 8080" dir=in action=allow protocol=TCP localport=8080
   netsh advfirewall firewall add rule name="Allow 5030" dir=in action=allow protocol=TCP localport=5030
   netsh advfirewall firewall add rule name="Allow 3000" dir=in action=allow protocol=TCP localport=3000
   ```

2. **或者图形界面设置**
   ```
   控制面板 → 系统和安全 → Windows Defender 防火墙 → 高级设置
   → 入站规则 → 新建规则 → 端口 → TCP → 特定本地端口 → 8080,5030,3000
   ```

## 🚨 常见问题排查

### 问题1: 获取不到IPv6地址
**可能原因:**
- 中国移动未开通IPv6服务
- 光猫IPv6功能未启用
- 路由器IPv6设置错误

**解决方案:**
1. 拨打10086确认IPv6服务状态
2. 重启光猫等待5分钟
3. 检查光猫和路由器IPv6配置

### 问题2: 有IPv6地址但无法访问外网
**可能原因:**
- IPv6网关未获取
- DNS解析问题
- 运营商网络问题

**解决方案:**
1. 检查IPv6默认路由
2. 手动设置IPv6 DNS: `2001:4860:4860::8888`
3. 重启网络适配器

### 问题3: 外网访问正常但服务无法访问
**可能原因:**
- 应用未绑定IPv6地址
- 防火墙阻挡
- 端口未监听

**解决方案:**
1. 修改应用配置绑定到`::`
2. 配置防火墙规则
3. 检查端口监听状态

## 📞 技术支持联系方式

### 中国移动客服
- **电话**: 10086
- **说明**: "需要开通IPv6服务，配置IPv6前缀委派"
- **关键词**: IPv6、前缀委派、2408段地址

### 光猫厂商技术支持
- **华为**: 400-822-9999
- **中兴**: 400-880-9999  
- **烽火**: 400-684-6888

## ✅ 配置成功验证

完成所有配置后，验证步骤：

1. **检查IPv6地址**
   ```cmd
   ipconfig | findstr "IPv6"
   # 应该看到 2408:8207 开头的地址
   ```

2. **测试IPv6连通性**
   ```cmd
   ping -6 ipv6.google.com
   # 应该有响应
   ```

3. **验证服务访问**
   ```cmd
   # 本地测试
   curl -6 http://[您的IPv6地址]:8080
   
   # 域名测试 (DDNS解析后)
   curl -6 http://ipv6.totodudu.com:8080
   ```

4. **在线IPv6测试**
   - 访问: https://test-ipv6.com/
   - 确保所有测试都通过

## 💡 额外建议

1. **网络拓扑建议**
   ```
   光猫(桥接模式) → 路由器(IPv6开启) → 服务器
   ```

2. **DNS配置建议**
   - 主DNS: `2001:4860:4860::8888` (Google)
   - 备DNS: `2400:3200::1` (阿里云)

3. **监控脚本**
   - 设置定时任务运行DDNS脚本
   - 添加IPv6地址变化监控
   - 配置服务可用性检查

按照这个指南逐步检查，应该能够解决您的IPv6远程访问问题。如果还有问题，请运行检查工具获取详细的诊断报告。 